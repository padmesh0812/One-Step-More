# Backend API URL (leave empty in local development to fallback to http://localhost:5000)
# In production on Vercel, set this to your deployed backend URL (e.g. https://one-step-more-api.onrender.com)
VITE_API_URL=http://localhost:5000
